// Stub for the `server-only` package under vitest. Next.js normally aliases
// this to a no-op on the server and throws on the client; in vitest we load
// server modules directly, so we mirror the server-side behaviour: nothing.
export {};
